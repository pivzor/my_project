<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Admin Panel{% endblock %}</title>
    {# Подключаем CSS из EasyAdmin #}
    <link rel="stylesheet" href="{{ asset('bundles/easyadmin/app.css') }}">
    <style>
        body {
            background-color: #f4f5f7;
            font-family: "Source Sans Pro", sans-serif;
        }
        .admin-login-box {
            width: 400px;
            margin: 100px auto;
            padding: 2rem;
            background: white;
            border-radius: 8px;
            box-shadow: 0 0 25px rgba(0,0,0,0.1);
        }
        .admin-login-box h1 {
            text-align: center;
            margin-bottom: 2rem;
            color: #343a40;
        }
        .form-control {
            width: 100%;
            padding: 0.5rem;
            margin-bottom: 1rem;
            border-radius: 4px;
            border: 1px solid #ced4da;
        }
        .btn-primary {
            background-color: #0d6efd;
            border-color: #0d6efd;
            color: #fff;
            padding: 0.5rem 1rem;
            width: 100%;
            border-radius: 4px;
            margin: 10px;
        }
    </style>
</head>
<body>
    {% block body %}{% endblock %}
</body>
</html>
